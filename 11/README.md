##  Part 2

- 千万不要想太多, P2 的 input 不会是带环的图，肯定是个 DAG，直接用 DP 就可以了，不会有爆炸的状态空间
- 其实 Part 1 已经有在暗示这点了
- 如果你使用 AI ，因为 AI 不会去读全部的输入文件，它会发现状态空间很复杂，并且每个从 SRV 出发的路径都会经过 FFT 和 DAC，然后疯狂做一堆无效的状态压缩 
 (其实就是可能会被「可能存在环」这点被骗了，作者太坏了哈哈哈)
- 看成对每个（带状态的）节点记分(能到 OUT 分数就 +1 )，然后做累加，题目就会很简单
